// The tail recursion take less time in morden compilers
// The caller had to do something after child call is called tail recursive
// The caller had nothing to do after child call is called tail recursive